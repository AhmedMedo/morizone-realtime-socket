# Socket.io Server Environment
PORT=3001

# Laravel API URL for token validation
LARAVEL_API_URL=http://localhost:8000

# Secret for internal communication (Laravel -> Socket.io)
# Generate a strong random string: openssl rand -hex 32
INTERNAL_SECRET=your-internal-secret-change-this
